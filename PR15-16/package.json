{
  "name": "notes-pwa",
  "version": "1.0.0",
  "description": "PWA Notes with HTTPS, App Shell, WebSocket and Push",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "https": "http-server --ssl --cert localhost.pem --key localhost-key.pem -p 3000"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.5.4",
    "web-push": "^3.6.6",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5"
  }
}