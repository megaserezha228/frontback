{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Frontend for auth-products API",
  "scripts": {
    "start": "npx live-server --port=8080"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}